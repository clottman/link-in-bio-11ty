{
  "name": "blankity-blank",
  "version": "1.0.0",
  "scripts": {
    "build": "eleventy --input=src --output=public",
    "serve": "eleventy --input=src --output=public --serve"
  },
  "dependencies": {
    "http-server": "^0.10.0",
    "@11ty/eleventy": "^0.3.5"
  }
}