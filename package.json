{
  "name": "eleventy-on-glitch",
  "description": "A starter template for eleventy static site generator on glitch"
  "version": "1.0.0",
  "scripts": {
    "build": "eleventy --input=src --output=dist",
    "start": "eleventy --input=src --output=dist --serve"
  },
  "dependencies": {
    "http-server": "^0.10.0",
    "@11ty/eleventy": "^0.3.5"
  }
}